/**
 * remove color as state and use directly
 *    <h1 style={{ color: props.color }}>
 */
/*
task 2
remove both total and packed state and use this function

function noOfPackedAndTotalItem(){
    const initialValue = 0;
    const sumWithInitial = items.reduce(
      (accumulator, currentValue) => accumulator + currentValue.packed?1:0,
      initialValue
    );
    return {packed:sumWithInitial, total:items.length};
  }
  let {packed,total}=noOfPackedAndTotalItem();
  */
